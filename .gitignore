# --- Raw Data Section ---

# 1. Ignore everything inside data/raw
data/raw/*

# 2. Allow subdirectories (CRITICAL: Git needs to peek inside folders)
!data/raw/*/

# 3. Un-ignore the tracker files anywhere in the structure
!data/raw/**/*.dvc

# 4. Un-ignore documentation
!data/raw/README.md
!data/raw/**/README.md